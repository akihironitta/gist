name: Test

on: [push]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  main:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9"]
        pl-release: ["stable", "master"]
        torch-release: ["lts", "stable", "nightly"]
    # https://github.com/conda-incubator/setup-miniconda/issues/179
    defaults:
      run:
        shell: bash -l {0}
    steps:
    - uses: actions/checkout@v3
    - uses: conda-incubator/setup-miniconda@v2
      with:
        python-version: ${{ matrix.python-version }}
        miniconda-version: latest
        activate-environment: test
        auto-update-conda: true
    - name: Install PyTorch ${{ matrix.torch-release }}
      run: |
        PYTORCH_CHANNEL=pytorch-${{ matrix.torch-release }}
        [ ${{ matrix.torch-release }} == 'stable' ] && PYTORCH_CHANNEL='pytorch'
        conda install -q pytorch torchvision cpuonly -c $PYTORCH_CHANNEL
    - name: Install dependencies
      run: |
        pip install -r requirements/all.txt
    - name: Run main.py
      run: |
        for i in $(find . -type f -name main.py)
        do
          echo "Testing ${i}"
          python ${i}
        done
