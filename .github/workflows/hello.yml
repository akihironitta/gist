name: Say hello

on:
  pull_request:
    branches:
      - main

jobs:
  track_pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/compare-actions
